// Revisão de ARRAYS
// criando um ARRAY de nomes

nomes = ["Ana", "Bruna", "Carol"];

// Exibindo todos os nomes

console.log(nomes);

// Exibindo apenas o primeiro nome 

console.log(nomes[0]);

// Exibindo o último nome 

console.log(nomes[2]);

// F5 para executar o código ou "CTRL + '" para abrir o terminal
// e executar o arquivo com "node revisao.js"
